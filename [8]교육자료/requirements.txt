# Tello EDU 드론 제어 프로젝트 - 필수 패키지
# 설치 명령: pip install -r requirements.txt

# ── 드론 제어 SDK ──────────────────────────────────────────────
djitellopy==2.5.0          # Tello 드론 통신 (UDP 기반)

# ── 영상 처리 ──────────────────────────────────────────────────
opencv-python>=4.8.0       # 영상 캡처, 화면 출력, 이미지 처리

# ── 객체 탐지 (2단계 이상 필요) ────────────────────────────────
ultralytics>=8.0.0         # YOLOv8 (자동으로 yolov8n.pt 모델 다운로드)

# ── GUI 앱 ─────────────────────────────────────────────────────
Pillow>=10.0.0             # tkinter ↔ OpenCV 이미지 브리지 (ImageTk)

# ── 기본 과학 연산 (numpy는 opencv 의존성에 포함) ───────────────
numpy>=1.24.0